/* EJERCICIOS DEL 1-4: float hazlo(char x, char y, char z);
void imprime_un_numero(int x);
La primera devuelve un valor entero, la segunda un numero largo. */

#include <stdio.h>

void print_msg(void);
main(){
	print_msg();
	system("PAUSE");
	return 0;
}
void print_msg(void)
{
	puts("this is a mesagge to print");
	
}